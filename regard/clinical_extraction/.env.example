# Copy to `.env` in this directory (gitignored) or set in repo-root `.env`.
# Never commit real keys.
# Used by evals/scripts/run_eval.py, evals/scripts/llm_judge_eval.py, and agents/chat_cli.py.

OPENAI_API_KEY=sk-your-key-here
